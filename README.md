# 你的压力地图

React + TypeScript + Vite 的纯前端心理自我探索问卷第一版。

## 本地运行

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```

Cloudflare Pages：Framework 选 Vite，Build command `npm run build`，Output directory `dist`。

当前版本不需要数据库，答案只在浏览器内计算。上线前建议做真实用户测试，并将其定位为自我探索而非诊断工具。
