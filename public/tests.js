const t=localStorage.getItem('psych_session'),a=document.querySelector('#app');document.querySelector('#logout').onclick=()=>{localStorage.removeItem('psych_session');location.href='/'};async function load(){if(!t){location.href='/login.html';return}const r=await fetch('/api/me',{headers:{authorization:'Bearer '+t}}),d=await r.json();if(!d.ok){localStorage.removeItem('psych_session');location.href='/login.html';return}const left=Math.max(0,d.expiresAt-Date.now()),h=Math.floor(left/3600000),m=Math.floor(left%3600000/60000);a.innerHTML='<div class="eyebrow">YOUR SPACE</div><h1>选择一个探索主题</h1><p class="lead small">授权剩余约 '+h+' 小时 '+m+' 分钟 · 已使用 '+d.devices.length+'/5 台设备</p><div class="test-list">'+d.tests.map(x=>'<article class="card"><span class="badge">即将上线</span><h2>'+x.title+'</h2><p>'+x.subtitle+'</p><p>'+x.description+'</p><button class="btn" onclick="location.href=\'/test/'+x.slug+'\'">进入 →</button></article>').join('')+'</div>'}load();
