const DK='psych_platform_device';function deviceToken(){let t=localStorage.getItem(DK);if(!t){t=crypto.randomUUID()+crypto.randomUUID();localStorage.setItem(DK,t)}return t}const f=document.querySelector('#form'),i=document.querySelector('#code'),m=document.querySelector('#msg');i.addEventListener('input',()=>{let v=i.value.toUpperCase().replace(/[^A-Z0-9]/g,'').slice(0,8);i.value=v.length>4?v.slice(0,4)+'-'+v.slice(4):v});f.addEventListener('submit',async e=>{e.preventDefault();m.textContent='正在验证…';try{const r=await fetch('/api/verify',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({code:i.value,deviceToken:deviceToken()})}),d=await r.json();if(!d.ok)throw Error(d.error||'验证失败');localStorage.setItem('psych_session',d.token);location.href='/tests.html'}catch(x){m.textContent=x.message}});
